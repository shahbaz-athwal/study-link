{
  "name": "study-link",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "frontend:build": "pnpm --filter frontend build",
    "frontend:install": "pnpm install --filter frontend"
  },
  "engines": {
    "node": "20.x"
  },
  "packageManager": "pnpm@9.14.2"
}
